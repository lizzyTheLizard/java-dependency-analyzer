name: Build
on: [push]
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Import GPG Key
        run: |
          gpg --list-public-keys
          export GPG_TTY=$(tty)
          ls -la ~/.gnupg/
          mkdir -p ~/.gnupg/
          mkdir -p ~/.gnupg/private-keys-v1.d
          echo "no-tty" >> ~/.gnupg/gpg.conf
          echo "${GPG_SIGNING_KEY}" > ~/.gnupg/import.key
          chmod -R 700 ~/.gnupg/
          gpgconf --kill gpg-agent
          gpg --import ~/.gnupg/import.key
        env:
          GPG_SIGNING_KEY: ${{ secrets.GPG_SIGNING_KEY }}
      - name: Import Maven Username / Passwords
        run: |
          echo -n "<settings><servers><server><id>ossrh</id><username>${OSSRH_USERNAME}</username><password>${OSSRH_PASSWORD}</password></server></servers></settings>" > ~/.m2/settings.xml
        env:
          OSSRH_USERNAME: ${{ secrets.OSSRH_USERNAME }}
          OSSRH_PASSWORD: ${{ secrets.OSSRH_PASSWORD }}
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'yarn'
          cache-dependency-path: frontend/yarn.lock
      - name: Install Frontend Dependencies
        working-directory: frontend
        run: yarn install
      - name: Test Frontend
        working-directory: frontend
        run: yarn test:nowatch
      - name: Lint Frontend
        working-directory: frontend
        run: yarn lint:nofix
      - name: Build Frontend
        working-directory: frontend
        run: yarn build
      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: 'maven'
      - name: Build Plugin
        working-directory: java-dependency-analyzer
        run: mvn --batch-mode install
      - name: Report Unit Tests
        uses: dorny/test-reporter@v1
        if: success() || failure()
        with:
          name: Unit Tests
          path: java-dependency-analyzer/target/surefire-reports/*.xml
          reporter: java-junit
          fail-on-error: true
      - name: Integration Test
        working-directory: maven-plugin-integration-test
        run: mvn --batch-mode verify