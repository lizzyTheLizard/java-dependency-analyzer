name: Build
on: [push]
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'yarn'
          cache-dependency-path: frontend/yarn.lock
      - name: Install Frontend Dependencies
        working-directory: frontend
        run: yarn install
      - name: Test Frontend
        working-directory: frontend
        run: yarn test:nowatch
      - name: Lint Frontend
        working-directory: frontend
        run: yarn lint:nofix
      - name: Build Frontend
        working-directory: frontend
        run: yarn build
      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: 'maven'
      - name: Build Plugin
        working-directory: dependency-analyzer
        run: ./mvnw --batch-mode install  -P build-executable-jar
      - name: Report Unit Tests
        uses: dorny/test-reporter@v1
        if: success() || failure()
        with:
          name: Unit Tests
          path: dependency-analyzer/target/surefire-reports/*.xml
          reporter: java-junit
          fail-on-error: true
      - name: Plugin Smoke Test
        working-directory: maven-plugin-integration-test
        run: ./mvnw --batch-mode install
      - name: Upload Plugin Smoke Test Result
        uses: actions/upload-artifact@v3
        with:
          name: Plugin Integration-Test Result
          path:  maven-plugin-integration-test/target/doc/index.html
      - name: CLI Smoke Test
        run: java -jar .\dependency-analyzer\target\dependency-analyzer-*-jar-with-dependencies.jar .\dependency-analyzer\target\classes
      - name: Upload CLI Smoke Test Result
        uses: actions/upload-artifact@v3
        with:
          name: CLI Integration-Test Result
          path:  maven-plugin-integration-test/target/doc/index.html
